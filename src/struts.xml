<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
    "http://struts.apache.org/dtds/struts-2.3.dtd">
 
<struts>

	<!-- about the picture size-->
   	
   <constant name="struts.multipart.maxSize" value="-1"/> 

   
   <package name="nus.iss.action" extends="struts-default">     
   		<!-- 
   		<action name="UploadAction" class="nus.iss.action.UploadAction">
   			<result name="success">/picUpload.jsp</result>
   			<result name="false">/false.jsp</result>
   		</action>
   		 -->
   		 <!-- This action could return json as image names but without coordinate -->
   		<action name="enquiryImagesWithCoordinate" class="enquiryImagesAction" method="enquiryImages"></action>
   		
   		<!-- This action could return json as image name with the coordinate -->
   		<action name="getImageInfoWithCoordinate" class="enquiryImagesAction" method="enquiryImagesWithCoordinate"></action>
   		
   		<!-- This action will return the size of the enquired images by given the names -->
   		<action name="getTotalSizeByImageNames" class="enquirySizeAction" method="getTotalSizeByImageNames"></action>
   		
   </package>

</struts>